Aquí tienes un archivo README.md profesional y bien estructurado, diseñado específicamente para destacar las habilidades técnicas que demuestras en este código (CSS Grid anidado, Variables CSS y Dark Mode).

Crea un archivo llamado README.md dentro de la carpeta Proyecto Grid - Bento\src y pega el siguiente contenido:

Bento Grid Layout - Apple Style 🍱
Este proyecto es una implementación de un diseño estilo "Bento Grid", popularizado por Apple en sus hojas de especificaciones (como en la presentación de los chips M1/M2).

El objetivo principal es demostrar el dominio de CSS Grid avanzado, utilizando grids anidados y diseño responsivo sin depender de frameworks externos.

🚀 Características Principales
Diseño 100% Responsivo:

📱 Móvil: Diseño de una sola columna fluida.

💻 Escritorio: Layout complejo de 3 columnas principales.

CSS Grid Anidado (Nested Grids): Arquitectura de contenedores padres e hijos para lograr alineaciones complejas.

Dark Mode Automático: Soporte nativo para tema oscuro utilizando prefers-color-scheme.

Variables CSS: Uso de Custom Properties (:root) para una gestión eficiente de colores, tipografías y espaciados.

Clases de Utilidad: Sistema modular para expandir filas y columnas (span-col-2, span-row-3).

🛠️ Tecnologías Utilizadas
HTML5: Semántico y estructurado.

CSS3:

Grid Layout & Flexbox.

Media Queries.

CSS Variables.

Metodología BEM (Bloque, Elemento, Modificador) simplificada.

📂 Estructura del Proyecto
El sistema de grid funciona mediante una jerarquía de tres niveles:

.bento-container: El contenedor padre que define las 3 columnas principales en escritorio.

.bento-column: Sub-contenedores que actúan como grids independientes para agrupar verticalmente las tarjetas.

.bento-item: Las tarjetas individuales que contienen la información y pueden expandirse.

Plaintext

📦 src
 ┣ 📂 assets
 ┃ ┗ 📂 images
 ┣ 📂 css
 ┃ ┣ 📜 styles.css      # Lógica del Grid y estilos visuales
 ┃ ┗ 📜 variables.css   # Paleta de colores y temas (Claro/Oscuro)
 ┗ 📜 index.html
🎨 Previsualización de Código
El proyecto utiliza variables para cambiar dinámicamente entre temas:

CSS

/* variables.css */
@media (prefers-color-scheme: dark) {
  :root {
    --color-page-bg: #000000;
    --color-card-bg: #1d1d1f;
    --color-text-primary: #f5f5f7;
  }
}
📦 Instalación y Uso
Clona el repositorio (o descarga los archivos).

Asegúrate de que las imágenes estén en la carpeta assets/images.

Abre el archivo index.html en tu navegador web preferido.

Prueba cambiar el tamaño de la ventana o activar el modo oscuro de tu sistema operativo para ver la adaptabilidad.

👨‍💻 Autor
Gonzalo Bernal

Proyecto creado con fines educativos para practicar maquetación avanzada con CSS Grid.