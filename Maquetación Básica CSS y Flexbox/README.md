# Maquetación Básica con CSS y Flexbox

Este proyecto es una práctica de maquetación web moderna que implementa una estructura de blog o sitio web estático. El objetivo principal es demostrar un sólido entendimiento de HTML5 semántico y el uso de **Flexbox** como pilar fundamental para el layout.

El diseño presenta una estructura clásica de tres columnas (header, contenido principal + barra lateral, y footer), con un tema oscuro y un diseño limpio y centrado.


## 🎨 Características Principales

* **Estructura Semántica:** Uso correcto de etiquetas HTML5 como `<header>`, `<main>`, `<aside>`, `<footer>`, `<nav>` y `<article>`.
* **Layout con Flexbox:** El layout principal, la navegación, la lista de artículos y los iconos sociales están construidos íntegramente con Flexbox.
* **Diseño Centrado:** El contenido principal está fijado a un ancho mínimo de `1000px` y máximo de `2000px`, centrado en la pantalla para una visualización cómoda en monitores grandes.
* **Tema Oscuro (Dark Mode):** Diseño elegante basado en una paleta de colores oscuros, definida con **Variables CSS** (`:root`) para un fácil mantenimiento y escalabilidad.
* **Componentes Reutilizables:** Estructura de "tarjetas" (`.article-card`) para mostrar artículos de forma modular.
* **Iconos y Fuentes:** Integración de fuentes desde Google Fonts (Roboto) e iconos de Font Awesome, ambos cargados desde un CDN.
* **Micro-interacciones:** Efectos `hover` sutiles en enlaces, botones y iconos sociales para mejorar la experiencia de usuario.
* **Base Responsiva:** Incluye una media query básica para reordenar el contenido en pantallas más pequeñas (menos de 768px).

## 🚀 Tecnologías Utilizadas

* **HTML5** (Semántico)
* **CSS3**
    * Flexbox
    * Variables CSS (Custom Properties)
    * Reset CSS básico (`box-sizing: border-box`)
* **Font Awesome** (para iconos)
* **Google Fonts** (para la tipografía)

---
*Autor. Gonzalo Bernal López.*
